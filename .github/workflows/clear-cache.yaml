name: Clear Cache
on:
  workflow_dispatch:
permissions:
  actions: write
jobs:
  clear-cache:
    name: Clear Cache
    runs-on: ubuntu-slim

    steps:
      - name: Clear caches
        uses: easimon/wipe-cache@v2